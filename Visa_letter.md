IMPORTANT: EXISTING VISA PDF TEMPLATE — DO NOT REDESIGN

I already have a working PHP script that generates the visa letter PDF using Dompdf.

The existing script is the reference implementation for the PDF and produces the layout/design that I want.

Use the existing visa-letter PHP code in the project as the source/template.

DO NOT create a new visa letter design from scratch.

DO NOT change the following unless technically necessary:

- A4 portrait layout
- Page dimensions
- Margins
- Background
- Typography
- Font sizes
- Paragraph spacing
- Logo position
- Church header
- Date position
- Subject
- Salutation
- Conference introduction
- Hilton Birmingham Metropole section
- Invitee details layout
- Closing paragraphs
- Signature position
- Signatory information
- Footer graphic
- Footer text
- Overall spacing and positioning

The existing script currently receives values using GET parameters such as:

$date
$inviteeName
$passportCountry
$passportNumber
$dateOfBirth
$dateOfIssue
$dateOfExpiry
$homeAddress

It then builds the HTML and passes it to Dompdf.

The new implementation should REUSE THIS SAME PDF GENERATION LOGIC.

The only major change should be where the data comes from.

CURRENT TEST VERSION:

GET parameters
    ↓
Existing PHP PDF template
    ↓
Dompdf
    ↓
PDF

NEW VERSION:

Database visa_requests record
    ↓
Existing PHP PDF template/layout
    ↓
Dompdf
    ↓
Save PDF to storage/visa/
    ↓
Preview / Email

Refactor the existing code into a reusable service/class if appropriate, but preserve the actual HTML/CSS template.

For example:

app/Services/VisaLetterPdfService.php

The service can have a method such as:

generate(VisaRequest $request): string

or an equivalent simple implementation appropriate for the existing project.

The important point is:

DO NOT rewrite the PDF design.

Extract/reuse the existing HTML/CSS from the working script.

Only replace the GET-based data with values from the database.

==================================================
LOCAL IMAGES
==================================================

The existing test script currently uses remote image URLs:

$logoUrl
$signatureUrl
$footerUrl

For the new system, use the corresponding local files already present in:

public/assets/images/

Do not redesign or replace the images.

Dompdf should use the local image files.

If Dompdf requires an absolute filesystem path, use the appropriate project path rather than a browser URL.

==================================================
PDF OUTPUT
==================================================

The existing script currently does:

$dompdf->stream(...)

Do NOT use streaming as the primary generation method anymore.

Instead:

1. Generate the PDF using the existing template.
2. Save the generated PDF to:

storage/visa/

3. Store the path in visa_requests.pdf_path.
4. Return the path.
5. A separate protected preview endpoint can stream the saved PDF to the browser.

This means the PDF generation logic remains in ONE place.

Preview, approval and email should all use the same PDF generation service.

There must NOT be:

- one PDF generator for preview
- another PDF generator for approval
- another PDF generator for email

ALL THREE MUST USE THE SAME SERVICE.

Example:

VisaLetterPdfService
        │
        ├── Preview
        │
        ├── Approve
        │
        └── Email attachment

This is important because the PDF sent to the applicant must be generated from exactly the same template that the administrator previews.

==================================================
DATA MAPPING
==================================================

Map database fields to the existing template:

visa_requests.full_name
    → $inviteeName

visa_requests.passport_country
    → $passportCountry

visa_requests.passport_number
    → $passportNumber

visa_requests.date_of_birth
    → $dateOfBirth

visa_requests.passport_issue_date
    → $dateOfIssue

visa_requests.passport_expiry_date
    → $dateOfExpiry

visa_requests.home_address
    → $homeAddress

The letter date should be generated by the system when the PDF is generated rather than submitted through GET parameters.

Do not expose database values through GET parameters.

==================================================
FINAL RULE
==================================================

Think of the existing PHP file as an already-approved PDF template.

We are NOT designing a new PDF.

We are converting the existing working script from:

"standalone GET-parameter PDF generator"

into:

"reusable database-driven PDF service".

Preserve the existing output as closely as possible.